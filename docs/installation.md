# Installation

While in early development we have not yet uploaded ElfAnalyzer to PyPI or Conda.
To install:

1. Clone/download this repo's code
2. Navigate to the cloned package
3. Run
    ```bash
    pip install -e .
    ```

This will install the package along with the dependencies listed in the pyproject.toml.